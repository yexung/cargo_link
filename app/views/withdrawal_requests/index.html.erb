<div class="min-h-screen bg-gray-50 py-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- 헤더 -->
    <div class="flex justify-between items-center mb-8">
      <div>
        <h1 class="text-3xl font-bold text-gray-900">환전 관리</h1>
        <p class="mt-2 text-gray-600">판매 수익을 환전 신청하고 처리 현황을 확인하세요</p>
      </div>
      <% if current_seller.can_request_withdrawal? %>
        <%= link_to "환전 신청", new_withdrawal_request_path, 
            class: "bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700" %>
      <% end %>
    </div>

    <!-- 잔고 정보 -->
    <div class="bg-white rounded-lg shadow-sm border p-6 mb-8">
      <h2 class="text-xl font-bold text-gray-900 mb-4">잔고 정보</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="text-center">
          <div class="text-3xl font-bold text-blue-600">
            <%= current_seller.formatted_balance %>
          </div>
          <div class="text-sm text-gray-600">현재 잔고</div>
        </div>
        <div class="text-center">
          <div class="text-3xl font-bold text-green-600">
            <%= number_with_delimiter(current_seller.total_earnings.to_i) %>원
          </div>
          <div class="text-sm text-gray-600">총 판매 수익</div>
        </div>
        <div class="text-center">
          <div class="text-3xl font-bold text-gray-600">
            <%= number_with_delimiter(current_seller.total_withdrawn.to_i) %>원
          </div>
          <div class="text-sm text-gray-600">환전 완료</div>
        </div>
      </div>
    </div>

    <!-- 환전 신청 내역 -->
    <div class="bg-white rounded-lg shadow-sm border">
      <div class="p-6 border-b">
        <h2 class="text-xl font-bold text-gray-900">환전 신청 내역</h2>
      </div>
      
      <% if @withdrawal_requests.any? %>
        <div class="overflow-x-auto">
          <table class="w-full">
            <thead class="bg-gray-50">
              <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">신청일시</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">신청금액</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">계좌정보</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">상태</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">처리일시</th>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
              <% @withdrawal_requests.each do |request| %>
                <tr class="hover:bg-gray-50">
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                    <%= request.requested_at.strftime("%Y-%m-%d %H:%M") %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <div class="text-sm font-medium text-gray-900">
                      <%= request.formatted_amount %>
                    </div>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                    <%= request.bank_name %><br>
                    <%= request.bank_account_number %><br>
                    <span class="text-gray-500"><%= request.account_holder_name %></span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full
                      <%= case request.status
                          when 'pending' then 'bg-yellow-100 text-yellow-800'
                          when 'approved' then 'bg-green-100 text-green-800'
                          when 'rejected' then 'bg-red-100 text-red-800'
                          end %>">
                      <%= case request.status
                          when 'pending' then '처리대기'
                          when 'approved' then '승인완료'
                          when 'rejected' then '거부됨'
                          end %>
                    </span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                    <%= request.processed_at&.strftime("%Y-%m-%d %H:%M") || '-' %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
        
        <!-- 페이지네이션 -->
        <div class="px-6 py-4 border-t">
          <%= paginate @withdrawal_requests if respond_to?(:paginate) %>
        </div>
      <% else %>
        <div class="p-6 text-center text-gray-500">
          <p>아직 환전 신청 내역이 없습니다.</p>
          <% if current_seller.can_request_withdrawal? %>
            <p class="mt-2">
              <%= link_to "첫 환전 신청하기", new_withdrawal_request_path, 
                  class: "text-blue-600 hover:text-blue-800" %>
            </p>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>