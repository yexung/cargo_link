<div class="min-h-screen bg-gray-50 py-8">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900">경매 수정</h1>
      <p class="mt-2 text-gray-600">경매 정보를 수정하세요</p>
    </div>

    <!-- Form -->
    <div class="bg-white shadow-sm rounded-lg">
      <%= form_with model: @auction, url: sellers_auction_path(@auction), method: :patch, local: true, class: "space-y-6 p-6" do |form| %>
        <% if @auction.errors.any? %>
          <div class="bg-red-50 border border-red-200 rounded-md p-4">
            <div class="flex">
              <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                </svg>
              </div>
              <div class="ml-3">
                <h3 class="text-sm font-medium text-red-800">오류가 발생했습니다</h3>
                <div class="mt-2 text-sm text-red-700">
                  <ul class="list-disc pl-5 space-y-1">
                    <% @auction.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                    <% end %>
                  </ul>
                </div>
              </div>
            </div>
          </div>
        <% end %>

        <!-- Vehicle Information (Read-only) -->
        <div class="bg-gray-50 rounded-lg p-4">
          <h3 class="text-lg font-medium text-gray-900 mb-4">선택된 차량</h3>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <div>
              <span class="text-sm text-gray-600">차량명:</span>
              <p class="font-medium text-gray-900"><%= @auction.vehicle.title %></p>
            </div>
            <div>
              <span class="text-sm text-gray-600">브랜드/모델:</span>
              <p class="font-medium text-gray-900"><%= @auction.vehicle.brand %> <%= @auction.vehicle.model %></p>
            </div>
            <div>
              <span class="text-sm text-gray-600">연식:</span>
              <p class="font-medium text-gray-900"><%= @auction.vehicle.year %>년</p>
            </div>
          </div>
        </div>

        <!-- Hidden vehicle_id -->
        <%= form.hidden_field :vehicle_id %>

        <!-- Basic Information -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div>
            <%= form.label :start_time, "경매 시작 시간", class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= form.datetime_local_field :start_time, 
                value: @auction.start_time&.strftime("%Y-%m-%dT%H:%M"), 
                class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" %>
            <% if @auction.active? %>
              <p class="mt-1 text-sm text-yellow-600">⚠ 진행 중인 경매는 시작 시간을 변경할 수 없습니다.</p>
            <% end %>
          </div>

          <div>
            <%= form.label :end_time, "경매 종료 시간", class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= form.datetime_local_field :end_time, 
                value: @auction.end_time&.strftime("%Y-%m-%dT%H:%M"), 
                class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" %>
          </div>

          <div>
            <%= form.label :reserve_price, "최소 입찰가 (원)", class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= form.number_field :reserve_price, 
                class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500", 
                placeholder: "예: 15000000", 
                min: 0,
                disabled: @auction.bids.any? %>
            <% if @auction.bids.any? %>
              <p class="mt-1 text-sm text-yellow-600">⚠ 입찰이 있는 경매는 최소 입찰가를 변경할 수 없습니다.</p>
            <% else %>
              <p class="mt-1 text-sm text-gray-500">이 가격 이상에서만 낙찰됩니다</p>
            <% end %>
          </div>
        </div>

        <!-- Status Information -->
        <div class="bg-blue-50 rounded-lg p-4">
          <h3 class="text-sm font-medium text-blue-900 mb-2">현재 경매 상태</h3>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-sm">
            <div>
              <span class="text-blue-600">상태:</span>
              <span class="font-medium text-blue-900">
                <%= case @auction.status
                    when 'upcoming' then '시작 예정'
                    when 'active' then '진행 중'
                    when 'ended' then '종료됨'
                    else @auction.status
                    end %>
              </span>
            </div>
            <div>
              <span class="text-blue-600">현재 최고 입찰가:</span>
              <span class="font-medium text-blue-900">
                <% if @auction.bids.any? %>
                  ₩<%= number_with_delimiter(@auction.bids.maximum(:amount)) %>
                <% else %>
                  없음
                <% end %>
              </span>
            </div>
            <div>
              <span class="text-blue-600">입찰자 수:</span>
              <span class="font-medium text-blue-900"><%= @auction.bids.count %>명</span>
            </div>
          </div>
        </div>

        <!-- Action Buttons -->
        <div class="flex justify-end space-x-4 pt-6 border-t">
          <%= link_to "취소", sellers_auction_path(@auction), class: "px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
          <%= form.submit "경매 수정", class: "px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        </div>
      <% end %>
    </div>

    <!-- Help Section -->
    <div class="mt-8 bg-amber-50 border border-amber-200 rounded-lg p-6">
      <h3 class="text-lg font-medium text-amber-900 mb-4">경매 수정 주의사항</h3>
      <div class="text-sm text-amber-800">
        <ul class="space-y-2">
          <li>• <strong>진행 중인 경매:</strong> 시작 시간은 변경할 수 없습니다.</li>
          <li>• <strong>입찰이 있는 경매:</strong> 최소 입찰가는 변경할 수 없습니다.</li>
          <li>• <strong>종료 시간:</strong> 현재 시간보다 늦어야 합니다.</li>
          <li>• <strong>주의:</strong> 수정사항은 즉시 반영되므로 신중하게 변경하세요.</li>
        </ul>
      </div>
    </div>
  </div>
</div>